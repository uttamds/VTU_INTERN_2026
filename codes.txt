2 sum code for JAVA
-------------------------


int left = 0;
right = arr.length()-1;

while(left <right)
{
  int curr_sum = arr[left]+arr[rigth];

  if (curr_sum == target)
  {
      System.out.println(arr[left]  + arr[right);
  }
  else if curr_sum <target   
  {
		left +=1;
  }
else
	{
	        right -=1;
	};
}
